import AiRecommendation from "@/Components/services/AiRecommendation";

export default function AiRecommendationPage() {
    return (
        <main className="container mx-auto p-4 min-h-screen">
            <AiRecommendation />
        </main>
    );
}